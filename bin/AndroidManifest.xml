<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comp3350.dpa"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/heisenberg"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
         <activity
            android:name=".presentation.AddPrescriptionActivity"
            android:label="@string/app_name" >
        </activity>
 		<activity
            android:name=".presentation.PrescriptionListActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".presentation.DoctorLoginActivity"
            android:label="@string/app_name" >
        </activity>
         <activity
            android:name=".presentation.PatientLoginActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".presentation.PatientInfoViewActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".presentation.PatientInfoActivity"
            android:label="@string/app_name" >
        </activity>
         <activity
            android:name=".presentation.PatientListActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".presentation.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.OrderPrescriptionActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
